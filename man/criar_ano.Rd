% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criar_ano.R
\name{criar_ano}
\alias{criar_ano}
\title{Cria colunas de ano com base na origem do dado (SIM, SIH ou SINAN)}
\usage{
criar_ano(df, banco)
}
\arguments{
\item{df}{Data frame com as colunas de datas.}

\item{banco}{String com o nome da base de origem, como "SIM", "SIH", "SINAN_VIOL" ou "SINAN_IEXO".}
}
\value{
Data frame com as colunas \code{ano} e/ou \code{ano_nasc} adicionadas.
}
\description{
Identifica colunas de datas relevantes de acordo com o banco de origem e gera colunas
padronizadas: \code{ano} (ano do evento principal) e \code{ano_nasc} (ano de nascimento).
}
\examples{
df <- data.frame(
  dt_nasc = as.Date(c("1980-01-01", "1990-02-02")),
  dt_obito = as.Date(c("2020-03-01", "2021-04-01")),
  dt_internacao = as.Date(c("2019-06-01", "2020-07-01"))
)
criar_ano(df, "SIM")
criar_ano(df, "SINAN_VIOL")
criar_ano(df, "SIH")

}
