% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soundex_vars.R
\name{soundex_vars}
\alias{soundex_vars}
\title{Geração de variáveis SoundexBR a partir de nomes completos}
\usage{
soundex_vars(df, col_name)
}
\arguments{
\item{df}{Um \code{data.frame} contendo a coluna de nomes completos a ser processada.}

\item{col_name}{Nome da coluna de nomes completos, passado como uma \code{string} (ex: \code{"nome_completo"}).}
}
\value{
Retorna o \code{data.frame} original com colunas adicionais:
\describe{
\item{\verb{<col_name>1}, \verb{<col_name>2}, \verb{<col_name>3}}{Partes do nome}
\item{\verb{<col_name>1_sound}, \verb{<col_name>2_sound}, \verb{<col_name>3_sound}}{Soundex de cada parte}
\item{\verb{<col_name>_sound}}{Concatenação dos três Soundex}
}
}
\description{
A função \code{soundex_vars()} automatiza a separação de componentes de um nome completo
(primeiro nome, nomes do meio e último sobrenome) e aplica o algoritmo SoundexBR
a cada componente. Os códigos Soundex resultantes podem ser usados em tarefas de
pareamento aproximado, deduplicação ou linkage de registros.

Esta função é útil em contextos de limpeza de dados, integração de bases e análise de dados
que contenham variações ortográficas de nomes próprios.
}
\details{
A função realiza os seguintes passos:
\enumerate{
\item Converte os caracteres para ASCII (removendo acentos).
\item Extrai o primeiro nome, os nomes intermediários e o último nome.
\item Aplica o algoritmo SoundexBR para cada uma das três partes.
\item Concatena os códigos SoundexBR das partes em uma nova variável do tipo \code{col_name_sound}.
}

A estrutura final do \code{data.frame} inclui:
\itemize{
\item col_name1, col_name2, col_name3 — partes do nome.
\item col_name1_sound, col_name2_sound, col_name3_sound — SoundexBR das partes.
\item col_name_sound — concatenação dos três códigos SoundexBR.
}

Os valores \code{"NANANA"} gerados pelo SoundexBR são convertidos para \code{NA}.
}
\examples{
\dontrun{
  df <- data.frame(nome_completo = c("João da Silva", "Maria de Souza"))
  df_processado <- soundex_vars(df, "nome_completo")
  head(df_processado)
}

}
