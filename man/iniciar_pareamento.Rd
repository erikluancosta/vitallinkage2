% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iniciar_pareamento.R
\name{iniciar_pareamento}
\alias{iniciar_pareamento}
\title{Inicia o pareamento determinístico por igualdade exata entre variáveis}
\usage{
iniciar_pareamento(df, variaveis)
}
\arguments{
\item{df}{Um \code{data.frame} ou \code{tibble} contendo os dados a serem processados.}

\item{variaveis}{Vetor de caracteres com os nomes das colunas que serão usadas para identificar registros iguais (chaves do pareamento).}
}
\value{
Um \code{data.frame} contendo as colunas \code{par_1} e \code{par_c1} para os registros que foram agrupados com base nas variáveis especificadas.
Os registros sem valores completos nas variáveis-chave permanecem com \code{NA} nessas colunas.
}
\description{
Esta função aplica uma etapa inicial de pareamento determinístico, identificando registros com valores idênticos em um conjunto de variáveis especificadas.
Para cada grupo de registros com valores completamente coincidentes nas variáveis selecionadas, é atribuído um identificador de grupo (\code{par_1}) e uma coluna auxiliar \code{par_c1}.
A função é útil como passo preliminar para processos de linkage determinístico em bases de dados.
}
\details{
As etapas incluem:
\itemize{
\item Remoção de registros com valores ausentes nas variáveis-chave.
\item Identificação de grupos com múltiplas ocorrências.
\item Geração de identificadores únicos (\code{par_1} e \code{par_c1}) por grupo.
\item Junção dos resultados ao \code{data.frame} original.
}
}
\examples{
library(dplyr)
df <- data.frame(
  nome = c("Ana", "Ana", "Bruno", "Carlos", "Carlos", NA),
  nascimento = c("2000-01-01", "2000-01-01", "1995-05-05", "1980-10-10", "1980-10-10", "2001-12-12")
)

resultado <- iniciar_pareamento(df, variaveis = c("nome", "nascimento"))
print(resultado)
}
